# diff-impact

Summarises changed files from a Git diff or, outside a Git worktree, inventories
the mounted target. Later versions can map those files to functions, findings,
harnesses, rules, and coverage.
